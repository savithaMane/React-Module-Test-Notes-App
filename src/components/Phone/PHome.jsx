/* eslint-disable no-unused-vars */
import React from "react";
import "./PStyles.css";

// PHome is a React component representing the home page of the phone interface in the application.

const PHome = () => {
	return <div className="phone_home"></div>;
};

export default PHome;
